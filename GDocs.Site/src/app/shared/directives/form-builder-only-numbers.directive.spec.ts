import { FormControlNumericOnlyDirective } from './only-numbers.directive';

describe('FormControlNumericOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlNumericOnlyDirective(null);
    expect(directive).toBeTruthy();
  });
});
