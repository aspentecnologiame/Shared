import { FormControlTextsOnlyDirective } from './form-builder-only-texts.directive';

describe('FormControlTextsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlTextsOnlyDirective(null);
    expect(directive).toBeTruthy();
  });
});
