export enum Status {
    Beta = 1,
    Vigente = 2,
    Inativo = 3
}

export enum TipoMensagem {
    Dispositivo = 1,
    Usuario = 2
}

export enum StatusMensagem {
    Lida = 1,
    NaoLida = 2
}

export enum SistemaOperacional {
    iOS = 1,
    Android = 2
}
