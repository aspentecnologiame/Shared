import { ModeloTerminalPipe } from './modelo-terminal.pipe';

describe('ModeloTerminalPipe', () => {
  it('create an instance', () => {
    const pipe = new ModeloTerminalPipe();
    expect(pipe).toBeTruthy();
  });
});
