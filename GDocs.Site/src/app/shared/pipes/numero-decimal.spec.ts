import { NumeroDecimalPipe } from './numero-decimal.pipe';

describe('NumeroDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
